/* const home = () => {
  const vista = "<h1>Home</h1>"
  const divHome = document.createElement('div')

  divHome.innerHTML = vista;
  let button = document.querySelector('#')
  button.classList.remove('button')
  button.classList.add('buttonblack')

  return divHome
}

const productos = () => {
  const vista = ""
  const divProductos = document.createElement('div')
  divProductos.classList.add('tamano')
  divProductos.innerHTML = vista;
  let button = document.querySelector('#productos')
  button.classList.remove('button')
  button.classList.add('buttonblack')

  return divProductos
}

const fotos = () => {
  const vista = ""
  const divFotos = document.createElement('div')
  divFotos.innerHTML = vista;
  let button = document.querySelector('#fotos')
  button.classList.remove('button')
  button.classList.add('buttonblack')

  return divFotos
}

const novedades = () => {
  const vista = ""
  const divNovedades = document.createElement('div')
  divNovedades.innerHTML = vista;
  let button = document.querySelector('#novedades')
  button.classList.remove('button')
  button.classList.add('buttonblack')

  return divNovedades
}


const contacto = () => {
  const vista = ""
  const divContacto = document.createElement('div')
  divContacto.innerHTML = vista;
  let button = document.querySelector('#contacto')
  button.classList.remove('button')
  button.classList.add('buttonblack')
  return divContacto
}

const notFound = () => {
  const vista = "<h2>Esta pagina no existe</h2>"
  const divNot = document.createElement('div')
  divNot.innerHTML = vista;

  return divNot
} */